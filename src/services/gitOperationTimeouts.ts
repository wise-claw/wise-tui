/** Git IPC 默认超时（毫秒）：避免 remote/凭据阻塞导致 UI 永久 loading。 */
export const GIT_STATUS_TIMEOUT_MS = 60_000;
export const GIT_STAGE_TIMEOUT_MS = 60_000;
export const GIT_COMMIT_TIMEOUT_MS = 120_000;
export const GIT_PULL_TIMEOUT_MS = 120_000;
export const GIT_PUSH_TIMEOUT_MS = 180_000;
export const GIT_FETCH_TIMEOUT_MS = 120_000;
