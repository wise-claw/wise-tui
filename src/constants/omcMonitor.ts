/** 与 `useMonitorOverview` 中合成监控项 `employeeId: omc-worker` 的展示名一致。 */
export const OMC_MONITOR_EMPLOYEE_NAME = "OMC员工";

/** 旧版批量 OMC 占位会话 id 前缀（已停止写入；仍用于解析历史 id 以跳转主会话）。 */
export const OMC_BATCH_HISTORY_SESSION_ID_PREFIX = "omc-batch-hist:";
