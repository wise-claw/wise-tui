/** worker 标签 `repositoryName` 片段：`{repoDisplay}/神经网:{label}` */
export const SESSION_FEEDBACK_LOOP_REPO_MARKER = "/神经网:";

export const SESSION_FEEDBACK_LOOP_DISPATCH_KIND_LABELS = {
  optimization: "优化",
  summary: "总结",
  habits: "习惯",
  config_patch: "配置补丁",
} as const;
