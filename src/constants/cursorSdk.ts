/** Cursor Local SDK Auto 模型的真实 id（`auto` 仅为 alias，不可直接传给 Local Agent）。 */
export const CURSOR_SDK_DEFAULT_MODEL = "default";

/** Local Agent 默认加载的 Cursor 设置层（project + user）。 */
export const CURSOR_SDK_SETTING_SOURCES = ["project", "user"] as const;
