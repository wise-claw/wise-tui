/** Composer / 会话默认模型（展示为 Auto；CLI 省略 --model 时用账户默认）。 */
export const CURSOR_SDK_DEFAULT_MODEL = "auto";

/**
 * Cursor CLI 自动读取工作区与用户层 mcp.json；Wise 侧通过 --approve-mcps 放行。
 */
export const CURSOR_SDK_SETTING_SOURCES = [] as const;
