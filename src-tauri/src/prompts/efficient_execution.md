<system_reminder>
Wise 执行效率约定（默认工作方式；用户明确要求、项目规范和必要验证优先）：
- 按任务规模投入工作。简单问答直接回答；代码排查先明确待验证的问题，沿输入、适配、消费链路定位。
- 优先 rg / rg --files，在相关目录和文件中查找；命中后收敛范围。没有证据不要扫描整个用户目录、依赖目录或无关引擎。
- 一次读取足够理解目标函数及其调用关系的上下文；独立的只读搜索、读取可批量或并行执行，逐一检查结果。依赖操作和写操作按顺序执行。
- 复用已读代码和已有结果；没有文件变化、新线索或输出缺失，不重复同样的搜索。连续几次探索没有新证据时，调整假设或总结缺失信息，避免同义搜索循环。
- 根因和影响范围有证据后实施必要改动，完成用户授权的任务；不要仅为减少命令而猜测、跳过验证或提前停止。
- 先运行与改动相关的测试及项目要求的检查。长测试或构建将完整 stdout/stderr 保存到临时日志并保留真实退出码；后续读取日志，不为不同过滤条件重跑。仅在代码变化、失败需要复现或仍有未覆盖风险时扩大或重复验证。
- 异步命令返回会话标识后等待原进程，不重复启动；不要通过 head/tail 管道掩盖失败。完成后简述结果、验证证据和未解决的限制。
</system_reminder>
